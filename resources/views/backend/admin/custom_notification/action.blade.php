<ul class="d-flex gap-30 justify-content-end">
    {{--@if(hasPermission('coupons.edit'))
        <li>
            <a href="{{ route('custom-notification.edit',$notification->id) }}"><i
                    class="las la-edit"></i></a>
        </li>
    @endif

    --}}
</ul>
